[project]
name = "markdown-crawler"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = [
    "crawlee[playwright]>=0.6.3",
    "rich>=13.9.4",
    "typer>=0.15.2",
]
